# Public API base (no trailing slash). Local dev:
VITE_API_BASE_URL=http://localhost:5000/api
# Production example: https://your-api.vercel.app/api
# The API server must send Access-Control-Allow-Origin for your frontend URL (CORS), or the browser will show "Failed to fetch".
